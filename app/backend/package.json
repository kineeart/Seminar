{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "gateway",
    "auth-service"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace gateway\" \"npm run dev --workspace auth-service\"",
    "dev:gateway": "npm run dev --workspace gateway",
    "dev:auth": "npm run dev --workspace auth-service",
    "start": "npm run dev",
    "test": "npm run test --workspace auth-service && npm run test --workspace gateway",
    "test:auth": "npm run test --workspace auth-service",
    "test:gateway": "npm run test --workspace gateway",
    "lint": "npm run lint --workspace gateway && npm run lint --workspace auth-service"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
